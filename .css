@import url('https://fonts.googleapis.com/css2?family=Poppins&family=Sofia+Sans:wght@700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: linear-gradient(90deg, #096c87, #35c8f0 100%);
}


#container {
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
}

.heading {
    color: #fff;
    font-size: 10rem;
    font-family: 'Roboto', sans-serif;
}

.sub-heading {
    color: #fff;
    text-align: center;
    font-weight: bold;
    font-size: 2,5rem;
    margin-bottom: 2rem;
    font-family: 'Roboto', sans-serif;
}

article {
    width: 80%;
    color: #fff;
    text-align: center;
    margin-bottom: 1rem;
    font-family:Arial, Helvetica, sans-serif;
}

.btn {
    cursor: pointer;
    margin: 0 0.5rem;
    font-weight: bold;
    padding: 1rem 2rem;
    border-radius: 5rem;
    text-transform: uppercase;
    transition: all 0.2s linear;
    font-family: Arial, Helvetica, sans-serif;
    border: 3px solid grey;
    color: black;
    background: linear-gradient(90deg,#027488 0%, #00a36d);
}

.btn:hover {
    border: 3px solid #FF4629;
    color: #FF4629;
}

    .buttons {
        display: flex;
        gap: 1.2rem;
        flex-direction: column;
    }

    /* STILE PER SMARTPHONE */
@media only screen and (max-width: 400px) {
     

    }
  
  /* PER TABLET */
  @media only screen and (min-width: 401px) and (max-width: 960px) {
    
  }
  
  
  /* DESKTOP */
  @media only screen and (min-width: 961px) {
    
    }
  
    
